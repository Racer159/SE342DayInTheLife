/**
 * Implements a task with a given response
 * @author Magikarpets (Team 3)
 *
 */
public interface Task{
	
	/**
	 * Captures an employees response when they decide to perform the task
	 */
	public void response(Employee e);
	
}
